{% extends 'base.html.twig' %}

{% block title %}Friend{% endblock %}

{% block body %}
    <h1>Friend</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ friend.id }}</td>
            </tr>
            <tr>
                <th>Validity</th>
                <td>{{ friend.validity ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_friend_index') }}">back to list</a>

    <a href="{{ path('app_friend_edit', {'id': friend.id}) }}">edit</a>

    {{ include('friend/_delete_form.html.twig') }}
{% endblock %}
