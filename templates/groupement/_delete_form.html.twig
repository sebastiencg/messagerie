<form method="post" action="{{ path('app_groupement_delete', {'id': groupement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groupement.id) }}">
    <button class="btn">Delete</button>
</form>
